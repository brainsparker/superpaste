{
  "name": "superpaste-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^22.15.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.0",
    "wrangler": "^3.0.0"
  }
}